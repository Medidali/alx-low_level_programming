#include "main.h"
#include <string.h>







char *str_concat(char *s1, char *s2)
{
char *concat;
int len1, len2;
if (s1 == NULL && s2 == NULL)
{
  return (' ');
}
len1 = strlen(s1);
len2 = strlen(s2);
concat = (char *)(sizeof(char) * (len1 + len2) + 1);
